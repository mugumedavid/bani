# FreeTDS configuration for Bani Docker image.
# Ensures stable connections for long-running data transfers.
[global]
    tds version = 7.3
    client charset = UTF-8
    text size = 2147483647
    # Disable idle timeout — transfers can take minutes
    timeout = 0
    connect timeout = 30
